apiVersion: v2
name: cheers
description: |
  Cheers — Rust gateway (server/) + React frontend + Postgres + RustFS (S3),
  the current external-agent-first architecture. External ACP agents connect
  via the connector and are NOT part of this chart.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Default image tag for gateway/frontend when not overridden in values.
appVersion: "latest"
keywords:
  - cheers
  - acp
  - gateway
home: https://github.com/haowei2000/Cheers
maintainers:
  - name: Cheers
