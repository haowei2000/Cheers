# 环境与密钥
.env
.env.local
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/

# 运行时与数据（设计书约定排除）
data/
tmp/
.tmp/
*.db
*.db-wal
*.db-shm

# 前端
node_modules/
frontend/dist/
*.tsbuildinfo
frontend/.env
frontend/.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store
.claude/scheduled_tasks.lock

# 测试与覆盖率
.coverage
htmlcov/
.pytest_cache/
.tox/

# 本地生成输出
/outputs/
/artifacts/

# 构建与日志
*.log
dist/
build/
*.egg-info/
*.egg

# 文档生成物（保留设计 docx，不提交生成物）
# docs/*.docx 可保留在仓库，按团队约定
docker-compose.yml

# 临时调试脚本（含明文 token，本地用）
/openclaw.sh

# 根目录 npm install 的副作用（仓库根不是 npm 项目）
/package.json
/package-lock.json

# OpenClaw plugin 构建产物（每次 release 时由 npm pack 生成，不入 git）
packages/openclaw-channel-agentnexus/*.tgz
release/*.tgz
release/*.tar.gz
.tmp_pytest/
.pytest_tmp/
tmp_pytest/
docker-compose.yml.bak-20260521-104115
release/agentnexus-opencode-amd64-latest-c0fe981b454e.tar.gz
