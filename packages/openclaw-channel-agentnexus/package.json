{
  "name": "openclaw-channel-agentnexus",
  "version": "0.1.0",
  "description": "OpenClaw channel plugin for AgentNexus — connects an OpenClaw agent to AgentNexus via the per-bot control + data WebSocket bridge.",
  "type": "module",
  "main": "./dist/lib.js",
  "types": "./dist/lib.d.ts",
  "exports": {
    ".": {
      "types": "./dist/lib.d.ts",
      "import": "./dist/lib.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit",
    "demo": "tsx src/demo.ts"
  },
  "keywords": ["openclaw", "channel", "plugin", "agentnexus", "websocket"],
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "~5.6.2",
    "vitest": "^2.1.4"
  },
  "openclaw": {
    "extensions": ["./dist/index.js"],
    "setupEntry": "./dist/setup.js",
    "channel": {
      "id": "agentnexus",
      "label": "AgentNexus",
      "selectionLabel": "AgentNexus (per-bot WebSocket bridge)",
      "detailLabel": "AgentNexus Bot",
      "docsPath": "/channels/agentnexus",
      "docsLabel": "agentnexus",
      "blurb": "Slack-like multi-channel chat with bots (via per-bot WS bridge).",
      "markdownCapable": true
    }
  }
}
