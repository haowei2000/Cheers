{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window capabilities: native notifications, in-app updates (check/install + the relaunch that follows), plus the shell permission for the bundled connector sidecar. App-defined commands (connector_* etc.) need no entries here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled",
    "updater:default",
    "process:allow-restart",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/cce-acp-connector",
          "sidecar": true,
          "args": true
        }
      ]
    }
  ]
}
