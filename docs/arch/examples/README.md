# Workbench Extension Examples

Legacy `.plugin.html` and standalone template JSON examples were removed in the unified extension cutover.

Current TypeScript, scene, seed, CSS, and manifest examples live in [`packages/cheers-workbench-sdk/examples`](../../../packages/cheers-workbench-sdk/examples). Build them with `cheers-workbench pack` to produce `.cheers-extension` packages.
