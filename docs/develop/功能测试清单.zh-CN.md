# 里程碑一 功能测试清单

> **语言**：中文 | [English](功能测试清单.md)

> 用于验收 M1 所有已实现功能的可访问性与行为。建议按顺序执行，并勾选通过项。

## 一、后端 API（可用 curl 或 http://localhost:8000/docs）

| 序号 | 功能 | 方法 | 路径/说明 | 通过 |
|-----|------|------|------------|------|
| 1 | 工作空间列表 | GET | `/api/v1/workspaces`，返回 data 数组 | □ |
| 2 | 频道列表 | GET | `/api/v1/channels` | □ |
| 3 | 创建频道 | POST | `/api/v1/channels`，body: workspace_id, name, type, purpose(可选) | □ |
| 4 | 频道成员列表 | GET | `/api/v1/channels/{channel_id}/members` | □ |
| 5 | 添加成员 | POST | `/api/v1/channels/{channel_id}/members`，body: member_id, member_type | □ |
| 6 | 移除成员 | DELETE | `/api/v1/channels/{channel_id}/members/{member_id}` | □ |
| 7 | 消息列表 | GET | `/api/v1/channels/{channel_id}/messages` | □ |
| 8 | 发送消息 | POST | `/api/v1/channels/{channel_id}/messages`，body: content, sender_id, sender_type, file_ids(可选) | □ |
| 9 | 频道上下文读取 | GET | `/api/v1/channels/{channel_id}/context` | □ |
| 10 | 频道上下文更新 | PUT | `/api/v1/channels/{channel_id}/context`，body: layer, content | □ |
| 11 | 文件上传 | POST | `/api/v1/files/upload?channel_id=&uploader_id=&filename=`，body 为文件二进制 | □ |
| 12 | 文件状态 | GET | `/api/v1/files/{file_id}/status` | □ |
| 13 | 健康检查 | GET | `/health` | □ |
| 13a | Agent Bridge 状态 | GET | `/api/v1/agent-bridge/status`，需 `X-Agent-Bridge-Token` | □ |

## 二、前端入口与易用性

| 序号 | 功能 | 操作步骤 | 通过 |
|-----|------|----------|------|
| 14 | 频道列表与切换 | 打开前端 → 左侧显示频道列表 → 点击频道切换 | □ |
| 15 | 发送消息 | 选中频道 → 输入框输入 → 发送或 Enter | □ |
| 16 | @ Bot 触发回复 | 在消息中输入 `@Coordinator 怎么用` → 收到内置协作助手回复 | □ |
| 17 | 帮助弹窗 | 左侧点击「帮助」→ 显示使用帮助与入口说明 | □ |
| 18 | 管理 - 创建项目 | 左侧「管理」→ 选择工作空间、填项目名称 → 创建 → 左侧出现新项目 | □ |
| 19 | 管理 - 添加成员 | 管理 → 添加成员：选择项目、填 member_id、选 user/bot → 添加 | □ |
| 20 | 管理 - 移除成员 | 管理 → 移除成员：选择项目、填 member_id → 移除 | □ |
| 21 | 管理 - API 文档链接 | 管理内「打开 API 文档」→ 新标签打开 /docs（开发环境需代理） | □ |
| 22 | 上传文件 | 选中频道 → 点击「上传」→ 选择 .txt/.md/.docx → 显示已附 → 发送消息（可带 file_ids） | □ |
| 23 | 频道上下文 | 选中频道 → 底部「频道上下文」→ 显示四层，可编辑并保存 | □ |
| 24 | @Coordinator 显示入口 | 在频道内输入 `@Coordinator 入口` → 回复中包含「管理」「上传」「频道上下文」等入口说明 | □ |

## 三、真实实现与架构

| 序号 | 检查项 | 说明 | 通过 |
|-----|--------|------|------|
| 25 | 无业务 Mock | 频道、成员、消息、上下文、文件均为真实持久化与 API | □ |
| 26 | Bot 适配器 | HTTP Bot 走 AIModel + PromptTemplate；Agent Bridge Bot 走 `/ws/agent-bridge/{control,data}`；内置 Coordinator 可回答帮助与协作问题 | □ |
| 27 | 种子数据 | 执行 seed 或 SEED_DATA=1 后，有默认工作空间、测试项目、@Coordinator | □ |

## 四、建议测试顺序

1. 启动后端与前端（或 Docker Compose）。
2. 若有需要，执行 `alembic upgrade head` 与 `python -m app.db.seed`（或 SEED_DATA=1）。
3. 按「一」用 Swagger 或 curl 验证 1～13。
4. 按「二」在前端逐项操作验证 14～24。
5. 按「三」确认 25～27。

全部勾选即表示 M1 功能测试通过。

---

## 五、门户阶段一（P1）功能测试

| 序号 | 功能 | 操作步骤 | 通过 |
|-----|------|----------|------|
| P1-1 | Coordinator 频道配置 | 创建或更新频道时设置 `auto_assist=true`，未 @ 消息可触发 Coordinator | □ |
| P1-2 | Coordinator LLM 配置 | 配置 `HELPER_LLM_*` 或管理端模型绑定后，确认 Coordinator 可正常回复 | □ |
| P1-3 | Coordinator 加入频道 | 管理 → 添加成员：member_id=bot-helper-001，member_type=bot | □ |
| P1-4 | @Coordinator 聚合 | 频道内输入 `@Coordinator` 问题 → 收到聚合或路由建议回复 | □ |
| P1-5 | 直接回答（未 @） | 开启 `auto_assist` 后，发消息不 @ 任何人 → Coordinator 回复业务问题 | □ |
| P1-6 | 自动接手 | Coordinator 回复含「建议 @xxx」→ 被建议 Bot 自动回复，显示「正在处理...」 | □ |
| P1-7 | 资源监控看板 | 规划项，不作为当前公开版本验收门槛 | □ |
| P1-8 | @Coordinator 帮助 | 频道内输入 `@Coordinator 怎么用` → 收到内置协作助手说明 | □ |
