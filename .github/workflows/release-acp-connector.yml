name: Release ACP connector

on:
  push:
    tags:
      - "agentnexus-acp-connector-v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to release, e.g. agentnexus-acp-connector-v0.1.0"
        required: true

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-acp-connector-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: "npm"
          cache-dependency-path: |
            packages/agentnexus-bridge-client/package-lock.json
            packages/agentnexus-acp-connector/package-lock.json

      - name: Verify npm token
        run: |
          set -euo pipefail
          if [ -z "${NODE_AUTH_TOKEN:-}" ]; then
            echo "::error::NPM_TOKEN secret is required to publish @agentnexus packages."
            exit 1
          fi

      - name: Resolve package versions
        id: versions
        env:
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            TAG="$INPUT_TAG"
          else
            TAG="$GITHUB_REF_NAME"
          fi
          BRIDGE_VERSION=$(node -p "require('./packages/agentnexus-bridge-client/package.json').version")
          ACP_VERSION=$(node -p "require('./packages/agentnexus-acp-connector/package.json').version")
          EXPECTED="agentnexus-acp-connector-v${ACP_VERSION}"
          if [ "$TAG" != "$EXPECTED" ]; then
            echo "::error::Tag '$TAG' does not match @agentnexus/acp-connector version (expected '$EXPECTED')."
            exit 1
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "bridge_version=$BRIDGE_VERSION" >> "$GITHUB_OUTPUT"
          echo "acp_version=$ACP_VERSION" >> "$GITHUB_OUTPUT"

      - name: Install bridge-client dependencies
        working-directory: packages/agentnexus-bridge-client
        run: npm ci

      - name: Verify bridge-client
        working-directory: packages/agentnexus-bridge-client
        run: |
          npm run lint
          npm test
          npm run build

      - name: Publish bridge-client to npm
        working-directory: packages/agentnexus-bridge-client
        env:
          BRIDGE_VERSION: ${{ steps.versions.outputs.bridge_version }}
        run: |
          set -euo pipefail
          if npm view "@agentnexus/bridge-client@${BRIDGE_VERSION}" version >/dev/null 2>&1; then
            echo "@agentnexus/bridge-client@${BRIDGE_VERSION} already exists on npm; skipping publish."
          else
            npm publish --access public --provenance
          fi

      - name: Pack bridge-client artifact
        id: bridge_pack
        working-directory: packages/agentnexus-bridge-client
        run: |
          set -euo pipefail
          TARBALL=$(npm pack --silent)
          STABLE="agentnexus-bridge-client.tgz"
          cp "$TARBALL" "$STABLE"
          echo "tarball=$TARBALL" >> "$GITHUB_OUTPUT"
          echo "stable=$STABLE" >> "$GITHUB_OUTPUT"

      - name: Install ACP connector dependencies
        working-directory: packages/agentnexus-acp-connector
        run: npm ci

      - name: Verify ACP connector
        working-directory: packages/agentnexus-acp-connector
        run: |
          npm run lint
          npm test
          npm run build

      - name: Rewrite ACP dependency for npm publish
        working-directory: packages/agentnexus-acp-connector
        env:
          BRIDGE_VERSION: ${{ steps.versions.outputs.bridge_version }}
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs = require("node:fs");
          const path = "package.json";
          const pkg = JSON.parse(fs.readFileSync(path, "utf8"));
          pkg.dependencies = pkg.dependencies || {};
          pkg.dependencies["@agentnexus/bridge-client"] = `^${process.env.BRIDGE_VERSION}`;
          fs.writeFileSync(path, `${JSON.stringify(pkg, null, 2)}\n`);
          NODE
          node -p "require('./package.json').dependencies['@agentnexus/bridge-client']"

      - name: Pack ACP connector artifact
        id: acp_pack
        working-directory: packages/agentnexus-acp-connector
        run: |
          set -euo pipefail
          TARBALL=$(npm pack --silent)
          STABLE="agentnexus-acp-connector.tgz"
          cp "$TARBALL" "$STABLE"
          echo "tarball=$TARBALL" >> "$GITHUB_OUTPUT"
          echo "stable=$STABLE" >> "$GITHUB_OUTPUT"

      - name: Publish ACP connector to npm
        working-directory: packages/agentnexus-acp-connector
        env:
          ACP_VERSION: ${{ steps.versions.outputs.acp_version }}
        run: |
          set -euo pipefail
          if npm view "@agentnexus/acp-connector@${ACP_VERSION}" version >/dev/null 2>&1; then
            echo "@agentnexus/acp-connector@${ACP_VERSION} already exists on npm; skipping publish."
          else
            npm publish --access public --provenance
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.versions.outputs.tag }}
          name: agentnexus-acp-connector v${{ steps.versions.outputs.acp_version }}
          draft: false
          prerelease: false
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            packages/agentnexus-bridge-client/${{ steps.bridge_pack.outputs.tarball }}
            packages/agentnexus-bridge-client/${{ steps.bridge_pack.outputs.stable }}
            packages/agentnexus-acp-connector/${{ steps.acp_pack.outputs.tarball }}
            packages/agentnexus-acp-connector/${{ steps.acp_pack.outputs.stable }}
